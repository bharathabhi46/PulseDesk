{
  "name": "pulsedesk",
  "version": "1.0.0",
  "private": true,
  "description": "Full-stack AI customer support automation platform built with MERN.",
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace pulsedesk-server\" \"npm run dev --workspace pulsedesk-client\"",
    "install:all": "npm install --workspaces",
    "build": "npm run build --workspace pulsedesk-client",
    "start": "npm run start --workspace pulsedesk-server",
    "lint": "npm run lint --workspace pulsedesk-client",
    "test": "npm run test --workspace pulsedesk-server"
  },
  "workspaces": [
    "pulseDeskFrontEnd",
    "pulseDeskBackEnd"
  ],
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
